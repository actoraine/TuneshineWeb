import { BaseTuneshineClient } from '../BaseTuneshineClient.js';

export class TuneshineV1_0_0Client extends BaseTuneshineClient {
  constructor(config, versionSettings) {
    super(config, versionSettings);
  }
}
